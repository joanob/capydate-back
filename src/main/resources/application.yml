spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB

springdoc:
  default-produces-media-type: application/json

server:
  servlet:
    content-path: /api/v1/

application:
  security:
    jwt:
      accessTokenCookieName: yobAuthToken
  admin:
    username: admin
    password: admin